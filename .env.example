# Backend API base (include /api/v1). Baked at build time — redeploy Vercel after changes.
#
# Local dev (backend on :8000):
VITE_API_URL=http://localhost:8000/api/v1
#
# Hosted (Vercel Production):
# VITE_API_URL=https://backend-production-847f.up.railway.app/api/v1
#
# Cloudinary: no frontend env vars. Sign params come from the API at upload time.
