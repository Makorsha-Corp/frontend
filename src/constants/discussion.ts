export const ORDER_DISCUSSION_SECTION_ID = 'order-discussion';
export const DISCUSSION_URL_HASH = 'discussion';

export function discussionMessageDomId(id: number) {
  return `discussion-message-${id}`;
}
